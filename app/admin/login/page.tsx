import LoginForm from "./loginForm";

export default async function LoginPage() {
	return (
		<div className="flex items-center justify-center h-fit min-h-lvh bg-background">
			<LoginForm />
		</div>
	);
}
